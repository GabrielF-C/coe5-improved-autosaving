```
usage: persist-autosaves.py [-h] [-n MAX_COUNT] [--ignore-process | --no-ignore-process]

options:
  -h, --help            show this help message and exit
  -n, --max-count MAX_COUNT
                        how many autosaves to keep
  --ignore-process, --no-ignore-process
                        if ignore, the program will not check if the 'COE5.exe' process is running
```